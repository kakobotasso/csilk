<h1>Listing contatos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Email</th>
    <th>Telefone</th>
    <th>Assunto</th>
    <th>Mensagem</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contatos.each do |contato| %>
  <tr>
    <td><%= contato.nome %></td>
    <td><%= contato.email %></td>
    <td><%= contato.telefone %></td>
    <td><%= contato.assunto %></td>
    <td><%= contato.mensagem %></td>
    <td><%= link_to 'Mostrar', [:admin, contato] %></td>
    <td><%= link_to 'Deletar', [:admin, contato], confirm: 'Tem certeza que deseja excluir este contato?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Contato', new_admin_contato_path %>
