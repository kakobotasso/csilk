<h1>Lista de fotos</h1>

<table>
  <tr>
    <th>Produto</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fotos.each do |foto| %>
  <tr>
    <td><%= foto.produto_id.nome %></td>
    <td><%= link_to 'Mostrar', [:admin, foto] %></td>
    <td><%= link_to 'Editar', edit_admin_foto_path(foto) %></td>
    <td><%= link_to 'Deletar', [:admin, foto], confirm: 'Tem certeza que deseja excluir esta foto?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova Foto', new_admin_foto_path %>
